---
phase: 01-memory-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - crates/amux-daemon/src/agent/episodic/retrieval.rs
  - crates/amux-daemon/src/agent/episodic/store.rs
  - crates/amux-daemon/src/agent/episodic/mod.rs
  - crates/amux-daemon/src/agent/goal_planner.rs
  - crates/amux-daemon/src/agent/goal_llm.rs
  - crates/amux-daemon/src/agent/system_prompt.rs
autonomous: true
requirements: [EPIS-02, EPIS-03, EPIS-04, EPIS-05, EPIS-06]

must_haves:
  truths:
    - "FTS5 queries return episodes ranked by BM25 relevance with recency weighting"
    - "Temporal filtering restricts results to episodes within a time range"
    - "Entity-aware retrieval finds episodes that mention specific files, tools, or libraries"
    - "Causal retrieval finds episodes whose root_cause matches a query"
    - "When a goal completes or fails, an episode is automatically recorded"
    - "Before planning a new goal, the top 5 relevant past episodes are surfaced in the planning context"
    - "Episodic context appears in the system prompt as a structured section"
    - "Hard cap of 5 episodes and 1500 token budget is enforced"
  artifacts:
    - path: "crates/amux-daemon/src/agent/episodic/retrieval.rs"
      provides: "FTS5 query building with BM25 ranking, temporal/entity/causal filtering, token budget enforcement"
      contains: "fn search_episodes"
    - path: "crates/amux-daemon/src/agent/goal_planner.rs"
      provides: "Episode recording hooks in complete_goal_run and fail_goal_run"
      contains: "record_goal_episode"
    - path: "crates/amux-daemon/src/agent/system_prompt.rs"
      provides: "Episodic context injection section in build_system_prompt"
      contains: "Past Experience"
  key_links:
    - from: "crates/amux-daemon/src/agent/goal_planner.rs"
      to: "crates/amux-daemon/src/agent/episodic/store.rs"
      via: "self.record_episode() call in complete_goal_run and fail_goal_run"
      pattern: "record_episode\\|record_goal_episode"
    - from: "crates/amux-daemon/src/agent/goal_llm.rs"
      to: "crates/amux-daemon/src/agent/episodic/retrieval.rs"
      via: "self.retrieve_relevant_episodes() call in request_goal_plan"
      pattern: "retrieve_relevant_episodes"
    - from: "crates/amux-daemon/src/agent/system_prompt.rs"
      to: "crates/amux-daemon/src/agent/episodic/retrieval.rs"
      via: "episodic_context parameter in build_system_prompt"
      pattern: "episodic_context"
---

<objective>
Build the FTS5 retrieval engine and wire episode recording into goal boundaries and episode surfacing into goal planning. After this plan, the agent automatically records episodes when goals complete/fail and retrieves relevant past episodes before planning new goals.

Purpose: This is the core intelligence loop -- episodes are recorded at goal boundaries and surfaced before planning, creating a feedback cycle where the agent learns from its past.

Output: Working retrieval engine, goal boundary hooks that record episodes, and planning injection that surfaces past experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-memory-foundation/01-RESEARCH.md
@.planning/phases/01-memory-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build FTS5 retrieval engine with multi-signal ranking</name>
  <files>
    crates/amux-daemon/src/agent/episodic/retrieval.rs,
    crates/amux-daemon/src/agent/episodic/mod.rs
  </files>
  <read_first>
    crates/amux-daemon/src/agent/episodic/mod.rs (types: Episode, EpisodeType, EpisodeOutcome, EpisodicConfig),
    crates/amux-daemon/src/agent/episodic/schema.rs (table definitions, FTS5 virtual table structure),
    crates/amux-daemon/src/agent/episodic/store.rs (existing store methods, row mapping pattern),
    crates/amux-daemon/src/history.rs (lines 140-200 for conn field, lines 2700-2900 for existing query patterns using self.conn.call()),
    crates/amux-daemon/src/agent/engine.rs (AgentEngine struct to understand self.history access)
  </read_first>
  <behavior>
    - Test 1: format_fts5_query escapes special FTS5 characters (double quotes, parentheses) in user input
    - Test 2: format_fts5_query converts space-separated words into FTS5 OR query terms
    - Test 3: format_episodic_context with empty episodes returns empty string
    - Test 4: format_episodic_context with 2 episodes (1 failure, 1 success) produces formatted text with WARNING and REFERENCE labels
    - Test 5: format_episodic_context truncates output when total exceeds max_injection_tokens * 4 (approx chars)
    - Test 6: compute_recency_weight returns 1.0 for episodes from today, ~0.5 for episodes 7 days old, ~0.1 for episodes 30+ days old
  </behavior>
  <action>
    1. Create `crates/amux-daemon/src/agent/episodic/retrieval.rs`:

    2. Implement `pub fn format_fts5_query(raw: &str) -> String`:
       - Strip FTS5 special characters: `"`, `(`, `)`, `*`, `{`, `}`, `[`, `]`, `:`, `^`
       - Split on whitespace, filter empty, join with " OR "
       - If result is empty, return `"*"` (match all)

    3. Implement `pub fn compute_recency_weight(episode_created_at: u64, now_ms: u64) -> f64`:
       - Calculate age in days: `(now_ms - episode_created_at) as f64 / (86400.0 * 1000.0)`
       - Use exponential decay: `(-0.05 * age_days).exp()` -- half-life ~14 days
       - Clamp to [0.01, 1.0]

    4. Implement `impl AgentEngine` block with `pub(crate) async fn retrieve_relevant_episodes(&self, query: &str, limit: usize) -> Result<Vec<Episode>>`:
       - Read config: `let config = self.config.read().await; let ep_config = &config.episodic;`
       - If `!ep_config.enabled` return empty vec
       - Let `effective_limit = limit.min(ep_config.max_retrieval_episodes)` (hard cap per EPIS-10)
       - Build FTS5 query via `format_fts5_query(query)`
       - Use `self.history.conn.call()` closure to execute:
         ```sql
         SELECT e.id, e.goal_run_id, e.thread_id, e.session_id, e.episode_type,
                e.summary, e.outcome, e.root_cause, e.entities, e.causal_chain,
                e.solution_class, e.duration_ms, e.tokens_used, e.confidence,
                e.created_at, e.expires_at,
                bm25(episodes_fts) as rank
         FROM episodes e
         JOIN episodes_fts ON e.rowid = episodes_fts.rowid
         WHERE episodes_fts MATCH ?1
           AND (e.expires_at IS NULL OR e.expires_at > ?2)
         ORDER BY rank
         LIMIT ?3
         ```
       - Pass params: fts5_query, now_ms (current time millis), effective_limit * 3 (over-fetch for re-ranking)
       - Map each row to Episode (parse `entities` TEXT as `serde_json::from_str::<Vec<String>>`, `causal_chain` TEXT as `serde_json::from_str::<Vec<CausalStep>>`, parse `episode_type` and `outcome` strings back to enums)
       - Re-rank by combined score: `bm25_rank * recency_weight` where `recency_weight = compute_recency_weight(episode.created_at, now_ms)`
       - Sort by combined score descending, take first `effective_limit` episodes
       - Return result

    5. Implement `pub(crate) async fn search_episodes_temporal(&self, query: &str, since_ms: u64, limit: usize) -> Result<Vec<Episode>>` (for EPIS-04):
       - Same as above but add `AND e.created_at >= ?4` with `since_ms` parameter

    6. Implement `pub(crate) async fn search_episodes_by_entity(&self, entity: &str, limit: usize) -> Result<Vec<Episode>>` (for EPIS-05):
       - Query: `SELECT * FROM episodes WHERE entities LIKE ?1 AND (expires_at IS NULL OR expires_at > ?2) ORDER BY created_at DESC LIMIT ?3`
       - Pass `%entity%` as the LIKE pattern (entities stored as JSON array TEXT, so LIKE matches substrings)

    7. Implement `pub fn format_episodic_context(episodes: &[Episode], max_tokens: usize) -> String` (for EPIS-03):
       - If episodes is empty, return empty String
       - Start with `"## Past Experience (Episodic Memory)\n"`
       - For each episode, format as:
         - Label: WARNING if outcome is Failure, CAUTION if Partial, REFERENCE if Success, NOTE if Abandoned
         - Line 1: `"{LABEL}: \"{summary}\" ({age} ago)"`
         - Line 2 (if root_cause Some): `"  Root cause: {root_cause}"`
         - Line 3 (if entities non-empty): `"  Entities: {entities joined by ', '}"`
         - Line 4: `"  Link: episode_{id}"`
         - Blank line separator
       - After each episode, check total char count against `max_tokens * 4` (APPROX_CHARS_PER_TOKEN)
       - If budget exceeded, append `"\n({remaining} more episodes omitted due to token budget)\n"` and break
       - Return the accumulated string

    8. Add `pub mod retrieval;` to `crates/amux-daemon/src/agent/episodic/mod.rs` if not already present.

    9. Add unit tests for format_fts5_query, compute_recency_weight, and format_episodic_context.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p amux-daemon --lib agent::episodic::retrieval -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `pub fn format_fts5_query`
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `async fn retrieve_relevant_episodes`
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `async fn search_episodes_temporal`
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `async fn search_episodes_by_entity`
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `pub fn format_episodic_context`
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `pub fn compute_recency_weight`
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `bm25(episodes_fts)` (FTS5 ranking)
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `LIMIT` (hard cap enforcement)
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `max_tokens` (token budget)
    - File `crates/amux-daemon/src/agent/episodic/retrieval.rs` contains `WARNING` and `REFERENCE` (output labels)
    - `cargo check -p amux-daemon` succeeds with exit code 0
  </acceptance_criteria>
  <done>FTS5 retrieval engine works with BM25 + recency re-ranking, supports temporal and entity-scoped queries, formats episodic context with severity labels, and enforces hard caps on episode count and token budget.</done>
</task>

<task type="auto">
  <name>Task 2: Wire episode recording into goal boundaries and episode surfacing into goal planning</name>
  <files>
    crates/amux-daemon/src/agent/episodic/store.rs,
    crates/amux-daemon/src/agent/goal_planner.rs,
    crates/amux-daemon/src/agent/goal_llm.rs,
    crates/amux-daemon/src/agent/system_prompt.rs
  </files>
  <read_first>
    crates/amux-daemon/src/agent/goal_planner.rs (full file -- find complete_goal_run at ~line 461, fail_goal_run at ~line 545, understand GoalRun fields and record_provenance_event pattern),
    crates/amux-daemon/src/agent/goal_llm.rs (lines 1-80 for request_goal_plan, lines 150-250 for request_goal_reflection and GoalReflectionResponse),
    crates/amux-daemon/src/agent/system_prompt.rs (full file -- understand build_system_prompt signature, parameters, and section assembly pattern),
    crates/amux-daemon/src/agent/episodic/store.rs (record_episode method signature),
    crates/amux-daemon/src/agent/episodic/retrieval.rs (retrieve_relevant_episodes and format_episodic_context signatures),
    crates/amux-daemon/src/agent/episodic/mod.rs (Episode, EpisodeType, EpisodeOutcome, CausalStep types),
    crates/amux-daemon/src/agent/types.rs (GoalRun struct definition, GoalRunStatus enum)
  </read_first>
  <action>
    1. Add a helper method to `crates/amux-daemon/src/agent/episodic/store.rs` in the `impl AgentEngine` block:

       `pub(crate) async fn record_goal_episode(&self, goal_run: &GoalRun, outcome: EpisodeOutcome) -> Result<()>`:
       - Extract from GoalRun: id, title (as summary), goal (as part of summary), thread_id, session_id
       - Set episode_type based on outcome: if Success then EpisodeType::GoalCompletion, else EpisodeType::GoalFailure
       - Build summary: `"{goal_run.title}: {goal_run.goal}"` -- truncate to 500 chars
       - Set root_cause: For failures, use `goal_run.last_error.clone().or(goal_run.failure_cause.clone())`; for success, None
       - Build entities: collect from goal_run.steps -- extract file paths from instructions (regex `(?:^|\\s)((?:[\\w.-]+/)*[\\w.-]+\\.\\w+)` ), tool names from step kind, session IDs. Prefix with type tags: `"file:{path}"`, `"step:{title}"`. Deduplicate.
       - Build causal_chain: For failures with steps, create CausalStep entries from each failed step: `{ step: step.title, cause: step.error.unwrap_or("unknown"), effect: "step failed" }`
       - Set duration_ms from `goal_run.duration_ms` or compute from `completed_at - started_at` if both present
       - Set created_at to current time millis
       - Set confidence to None (will be populated by Phase 2)
       - Call `self.record_episode(episode).await`

       Also add `pub(crate) async fn record_session_end_episode(&self, thread_id: &str, session_summary: &str, entities: Vec<String>) -> Result<()>` (for EPIS-08):
       - Create Episode with episode_type=SessionEnd, outcome=Success, summary=session_summary, entities=entities
       - Call `self.record_episode(episode).await`

    2. In `crates/amux-daemon/src/agent/goal_planner.rs`:

       In `complete_goal_run()` method (around line 461+), AFTER the existing `self.record_provenance_event(...)` call that logs goal completion, add:
       ```rust
       // Record episodic memory for goal completion (Phase 1: Memory Foundation)
       if let Err(e) = self.record_goal_episode(&updated, super::episodic::EpisodeOutcome::Success).await {
           tracing::warn!("Failed to record episodic memory for completed goal {}: {e}", goal_run_id);
       }
       ```
       Find the exact insertion point by locating the `record_provenance_event` call inside `complete_goal_run` and adding the episodic call immediately after it. Use `updated` (the GoalRun variable used in the provenance call) as the goal_run parameter.

       In `fail_goal_run()` method (around line 545+), AFTER the existing `self.record_provenance_event(...)` call that logs goal failure, add:
       ```rust
       // Record episodic memory for goal failure (Phase 1: Memory Foundation)
       if let Err(e) = self.record_goal_episode(&updated, super::episodic::EpisodeOutcome::Failure).await {
           tracing::warn!("Failed to record episodic memory for failed goal {}: {e}", goal_run_id);
       }
       ```
       Find the exact insertion point by locating the `record_provenance_event` call inside `fail_goal_run` and adding immediately after. Use `updated` as the variable (match whatever variable name the existing code uses for the goal run at that point).

       Also add the necessary import at the top of goal_planner.rs if needed.

    3. In `crates/amux-daemon/src/agent/goal_llm.rs`:

       In `request_goal_plan()` method, BEFORE the prompt is built (before the `let prompt = format!(...)` line), add episode retrieval:
       ```rust
       // Surface relevant past episodes before planning (Phase 1: Memory Foundation - EPIS-03)
       let episodic_context = match self.retrieve_relevant_episodes(&goal_run.goal, 5).await {
           Ok(episodes) if !episodes.is_empty() => {
               let config = self.config.read().await;
               let max_tokens = config.episodic.max_injection_tokens;
               drop(config);
               super::episodic::retrieval::format_episodic_context(&episodes, max_tokens)
           }
           Ok(_) => String::new(),
           Err(e) => {
               tracing::warn!("Episodic retrieval failed for goal plan: {e}");
               String::new()
           }
       };
       ```
       Then append the episodic context to the planning prompt. Find where the prompt string is assembled and add after the main instructions but before the JSON schema:
       ```rust
       if !episodic_context.is_empty() {
           prompt.push_str("\n\n");
           prompt.push_str(&episodic_context);
           prompt.push_str("\nConsider the above past experiences when planning. Avoid approaches that previously failed unless circumstances have changed.\n");
       }
       ```
       NOTE: The exact injection point depends on how the prompt is built. If it uses `format!()`, you may need to refactor to use `String::push_str()` or insert the context into the format arguments. Read the full method to determine the best approach.

    4. In `crates/amux-daemon/src/agent/system_prompt.rs`:

       Add `episodic_context: Option<&str>` as a new parameter to `build_system_prompt()` function signature. Add it after the existing `learned_patterns: Option<&str>` parameter.

       Inside the function body, after the section where `learned_patterns` is appended (find the `if let Some(lp) = learned_patterns` block), add:
       ```rust
       if let Some(ec) = episodic_context {
           if !ec.is_empty() {
               prompt.push_str("\n\n");
               prompt.push_str(ec);
           }
       }
       ```

       Then find ALL callers of `build_system_prompt` in the codebase and update them to pass the new parameter. Most callers should pass `None` for episodic_context since only the goal planning path provides it. Search for `build_system_prompt(` across the crate to find all call sites. Each call site needs the additional `None` argument appended.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo check -p amux-daemon 2>&1 | tail -10 && cargo test -p amux-daemon --lib agent::episodic -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/amux-daemon/src/agent/episodic/store.rs` contains `async fn record_goal_episode`
    - File `crates/amux-daemon/src/agent/episodic/store.rs` contains `async fn record_session_end_episode`
    - File `crates/amux-daemon/src/agent/goal_planner.rs` contains `record_goal_episode` (episode recording at goal boundary)
    - File `crates/amux-daemon/src/agent/goal_planner.rs` contains `EpisodeOutcome::Success` (success path)
    - File `crates/amux-daemon/src/agent/goal_planner.rs` contains `EpisodeOutcome::Failure` (failure path)
    - File `crates/amux-daemon/src/agent/goal_llm.rs` contains `retrieve_relevant_episodes` (episode surfacing before planning)
    - File `crates/amux-daemon/src/agent/goal_llm.rs` contains `format_episodic_context` (formatting for injection)
    - File `crates/amux-daemon/src/agent/system_prompt.rs` contains `episodic_context` parameter in build_system_prompt signature
    - `cargo check -p amux-daemon` succeeds with exit code 0
    - `cargo test -p amux-daemon` all tests pass (no regressions)
  </acceptance_criteria>
  <done>Goal completion/failure automatically records structured episodes. Goal planning retrieves top 5 relevant past episodes via FTS5 and injects them into the planning context. System prompt supports episodic context injection. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. `cargo check -p amux-daemon` compiles without errors
2. `cargo test -p amux-daemon --lib agent::episodic` -- all episodic tests pass
3. `cargo test -p amux-daemon` -- all existing daemon tests still pass
4. Grep confirms: `record_goal_episode` called in both `complete_goal_run` and `fail_goal_run`
5. Grep confirms: `retrieve_relevant_episodes` called in `request_goal_plan`
6. Grep confirms: `episodic_context` parameter exists in `build_system_prompt` signature
</verification>

<success_criteria>
- FTS5 retrieval engine returns ranked episodes with BM25 + recency scoring
- Temporal, entity-aware, and causal retrieval queries are all functional
- Goal completions and failures automatically generate episode records
- Goal planning surfaces relevant past episodes (max 5, max 1500 tokens)
- System prompt includes episodic context section when available
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-foundation/01-02-SUMMARY.md`
</output>
