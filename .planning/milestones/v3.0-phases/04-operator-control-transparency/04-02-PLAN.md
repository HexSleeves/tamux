---
phase: 04-operator-control-transparency
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/amux-daemon/src/agent/autonomy.rs
  - crates/amux-daemon/src/agent/authorship.rs
  - crates/amux-daemon/src/agent/types.rs
  - crates/amux-daemon/src/agent/work_context.rs
  - crates/amux-daemon/src/agent/goal_planner.rs
  - crates/amux-daemon/src/agent/mod.rs
  - crates/amux-protocol/src/messages.rs
  - crates/amux-daemon/src/server.rs
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTH-01, AUTH-02]

must_haves:
  truths:
    - "Operator can set autonomy level (autonomous/aware/supervised) when starting a goal run"
    - "Autonomous mode suppresses intermediate events, operator sees only final report"
    - "Aware mode reports on milestones (current default behavior)"
    - "Supervised mode reports every significant step and waits for operator acknowledgment"
    - "Goal outputs carry authorship metadata: operator, agent, or joint"
    - "Attribution is metadata on GoalRun output, not inline commentary"
  artifacts:
    - path: "crates/amux-daemon/src/agent/autonomy.rs"
      provides: "AutonomyLevel enum, event filtering logic, acknowledgment gate helper"
      exports: ["AutonomyLevel", "should_emit_event", "requires_acknowledgment"]
    - path: "crates/amux-daemon/src/agent/authorship.rs"
      provides: "AuthorshipTag enum, GoalRun output tagging logic"
      exports: ["AuthorshipTag", "classify_authorship"]
    - path: "crates/amux-daemon/src/agent/types.rs"
      provides: "AutonomyLevel and AuthorshipTag on GoalRun"
      contains: "autonomy_level"
    - path: "crates/amux-protocol/src/messages.rs"
      provides: "autonomy_level field on AgentStartGoalRun"
      contains: "autonomy_level"
  key_links:
    - from: "crates/amux-protocol/src/messages.rs"
      to: "crates/amux-daemon/src/server.rs"
      via: "AgentStartGoalRun autonomy_level passed through to start_goal_run"
      pattern: "autonomy_level"
    - from: "crates/amux-daemon/src/agent/work_context.rs"
      to: "crates/amux-daemon/src/agent/autonomy.rs"
      via: "should_emit_event check before event_tx.send in emit_goal_run_update"
      pattern: "should_emit_event"
    - from: "crates/amux-daemon/src/agent/goal_planner.rs"
      to: "crates/amux-daemon/src/agent/autonomy.rs"
      via: "Acknowledgment gate in supervised mode at step boundaries"
      pattern: "requires_acknowledgment"
---

<objective>
Implement the per-goal autonomy dial and shared authorship metadata.

Purpose: Let the operator control how much the agent reports during goal runs (autonomous/aware/supervised) and see attribution on goal outputs showing what came from operator vs agent.
Output: AutonomyLevel enum with event filtering, supervised mode acknowledgment gates, AuthorshipTag on GoalRun completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-operator-control-transparency/04-RESEARCH.md
@.planning/phases/04-operator-control-transparency/04-01-SUMMARY.md

<interfaces>
<!-- Existing types the executor needs -->

From crates/amux-protocol/src/messages.rs:
```rust
// ClientMessage variant to extend:
AgentStartGoalRun {
    goal: String,
    title: Option<String>,
    thread_id: Option<String>,
    session_id: Option<String>,
    priority: Option<String>,
    client_request_id: Option<String>,
    // ADD: autonomy_level: Option<String>,
},
```

From crates/amux-daemon/src/agent/types.rs:
```rust
pub struct GoalRun {
    pub id: String,
    pub status: GoalRunStatus,
    // ... existing fields ...
    pub steps: Vec<GoalRunStep>,
    pub events: Vec<GoalRunEvent>,
    // After Plan 01: total_prompt_tokens, total_completion_tokens, estimated_cost_usd
    // ADD: autonomy_level, authorship_tag
}

pub enum AgentEvent {
    GoalRunUpdate { goal_run_id, status, current_step_index, message, goal_run },
    // ...
}
```

From crates/amux-daemon/src/agent/work_context.rs:
```rust
pub(super) fn emit_goal_run_update(&self, goal_run: &GoalRun, message: Option<String>) {
    let _ = self.event_tx.send(AgentEvent::GoalRunUpdate {
        goal_run_id: goal_run.id.clone(),
        status: goal_run.status,
        current_step_index: Some(goal_run.current_step_index),
        message,
        goal_run: Some(goal_run.clone()),
    });
}
```

From crates/amux-daemon/src/server.rs:
```rust
// L1820-1847: AgentStartGoalRun handling
ClientMessage::AgentStartGoalRun { goal, title, thread_id, session_id, priority, client_request_id } => {
    let goal_run = agent.start_goal_run(goal, title, thread_id, session_id, priority.as_deref(), client_request_id).await;
    // ...
}
```

From crates/amux-daemon/src/agent/goal_planner.rs:
```rust
// GoalRun status: AwaitingApproval already exists for confidence-gated plans
// L588: complete_goal_run does reflection, memory, skill gen, persist
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create autonomy and authorship modules with types and logic</name>
  <read_first>
    - crates/amux-daemon/src/agent/types.rs (GoalRun struct, GoalRunStatus enum, AgentEvent enum, AwaitingApproval status)
    - crates/amux-daemon/src/agent/work_context.rs (emit_goal_run_update implementation)
    - crates/amux-daemon/src/agent/mod.rs (module declarations)
    - crates/amux-daemon/src/agent/goal_planner.rs (complete_goal_run, step advancement logic)
    - crates/amux-protocol/src/messages.rs (AgentStartGoalRun variant)
    - crates/amux-daemon/src/server.rs (L1820-1847 AgentStartGoalRun handling, start_goal_run call)
    - crates/amux-daemon/src/agent/engine.rs (start_goal_run method signature)
  </read_first>
  <files>
    crates/amux-daemon/src/agent/autonomy.rs,
    crates/amux-daemon/src/agent/authorship.rs,
    crates/amux-daemon/src/agent/types.rs,
    crates/amux-daemon/src/agent/mod.rs
  </files>
  <behavior>
    - Test: AutonomyLevel default is Aware
    - Test: should_emit_event("completed", Autonomous) returns true
    - Test: should_emit_event("step_started", Autonomous) returns false
    - Test: should_emit_event("step_started", Aware) returns true
    - Test: should_emit_event("step_detail", Aware) returns false
    - Test: should_emit_event("step_detail", Supervised) returns true
    - Test: requires_acknowledgment(Supervised) returns true
    - Test: requires_acknowledgment(Aware) returns false
    - Test: requires_acknowledgment(Autonomous) returns false
    - Test: AutonomyLevel serde round-trips correctly ("autonomous", "aware", "supervised")
    - Test: AutonomyLevel::from_str("supervised") returns Supervised, unknown string returns Aware (default)
    - Test: AuthorshipTag serde round-trips correctly ("operator", "agent", "joint")
    - Test: classify_authorship with operator-only input returns Operator
    - Test: classify_authorship with agent synthesis returns Agent
    - Test: classify_authorship with both returns Joint
    - Test: GoalRun with new autonomy_level and authorship_tag fields round-trips through serde (backward compatible)
  </behavior>
  <action>
    1. Create `crates/amux-daemon/src/agent/autonomy.rs`:
       - Define `AutonomyLevel` enum with `Autonomous`, `Aware`, `Supervised` variants. Derive `Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize` with `#[serde(rename_all = "snake_case")]`. Default impl returns `Aware`.
       - Add `pub fn from_str_or_default(s: &str) -> Self` that parses "autonomous"/"aware"/"supervised" (case-insensitive), defaulting to Aware for unknown strings.
       - Define `pub fn should_emit_event(level: AutonomyLevel, event_kind: &str) -> bool`:
         - Autonomous: only "completed", "failed", "budget_alert" events pass
         - Aware: everything except "step_detail" passes (this is current behavior)
         - Supervised: everything passes
       - Define `pub fn requires_acknowledgment(level: AutonomyLevel) -> bool`: true only for Supervised.
       - Add unit tests for all above.

    2. Create `crates/amux-daemon/src/agent/authorship.rs`:
       - Define `AuthorshipTag` enum with `Operator`, `Agent`, `Joint` variants. Derive `Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize` with `#[serde(rename_all = "snake_case")]`. Default impl returns `Joint`.
       - Define `pub fn classify_authorship(has_operator_goal_text: bool, has_agent_synthesis: bool) -> AuthorshipTag`:
         - Both true -> Joint
         - Only operator -> Operator (rare: goal run requested but agent did nothing)
         - Only agent -> Agent (rare: agent self-initiated goal)
         - Neither -> Joint (fallback)
       - Add unit tests.

    3. In `types.rs`:
       - Add to GoalRun struct: `#[serde(default)] pub autonomy_level: super::autonomy::AutonomyLevel` and `#[serde(default, skip_serializing_if = "Option::is_none")] pub authorship_tag: Option<super::authorship::AuthorshipTag>`.

    4. In `mod.rs`: Add `mod autonomy;` and `mod authorship;` declarations. These are internal daemon modules, not pub.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p tamux-daemon autonomy -- --nocapture 2>&1 | tail -20 && cargo test -p tamux-daemon authorship -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - autonomy.rs and authorship.rs exist with all types and functions
    - GoalRun has autonomy_level and authorship_tag fields with serde(default) for backward compatibility
    - All autonomy and authorship unit tests pass
    - `cargo check -p tamux-daemon` compiles
  </acceptance_criteria>
  <done>AutonomyLevel and AuthorshipTag types exist with event filtering and classification logic. GoalRun struct extended. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire autonomy into IPC, event emission, and goal completion with authorship</name>
  <read_first>
    - crates/amux-protocol/src/messages.rs (AgentStartGoalRun at L261-268)
    - crates/amux-daemon/src/server.rs (L1820-1847 goal run start handling)
    - crates/amux-daemon/src/agent/engine.rs (start_goal_run method)
    - crates/amux-daemon/src/agent/work_context.rs (emit_goal_run_update)
    - crates/amux-daemon/src/agent/goal_planner.rs (plan_and_start_goal_run, advance step logic, complete_goal_run)
    - crates/amux-daemon/src/agent/types.rs (after Task 1 changes)
    - crates/amux-daemon/src/agent/autonomy.rs (after Task 1)
    - crates/amux-daemon/src/agent/authorship.rs (after Task 1)
  </read_first>
  <files>
    crates/amux-protocol/src/messages.rs,
    crates/amux-daemon/src/server.rs,
    crates/amux-daemon/src/agent/work_context.rs,
    crates/amux-daemon/src/agent/goal_planner.rs
  </files>
  <action>
    1. In `crates/amux-protocol/src/messages.rs`:
       - Add `#[serde(default, skip_serializing_if = "Option::is_none")] autonomy_level: Option<String>` field to the `AgentStartGoalRun` variant. This is a string on the wire ("autonomous"/"aware"/"supervised") to keep the protocol crate dependency-free.

    2. In `crates/amux-daemon/src/server.rs`:
       - In the `AgentStartGoalRun` match arm (L1820), destructure the new `autonomy_level` field.
       - Pass it through to `agent.start_goal_run(...)`. This requires updating the `start_goal_run` method signature to accept `autonomy_level: Option<String>`.

    3. Find and update `start_goal_run` in the engine/goal_planner to:
       - Accept the new `autonomy_level: Option<String>` parameter.
       - Parse it via `AutonomyLevel::from_str_or_default(s)` and set it on the new GoalRun.
       - Default to `AutonomyLevel::Aware` per locked decision.

    4. In `work_context.rs`, modify `emit_goal_run_update`:
       - Before sending the event, check `should_emit_event(goal_run.autonomy_level, event_kind)`.
       - Derive event_kind from the GoalRunStatus: "completed" for Completed/Failed, "step_started" for Running with step changes, "planning" for Planned. Use a helper function to map.
       - ALWAYS emit "completed" and "failed" events regardless of autonomy level (safety net).
       - For Supervised mode, after emitting the update, if `requires_acknowledgment(goal_run.autonomy_level)` and the event is a step start, set the goal run to AwaitingApproval status.

    5. In `goal_planner.rs`:
       - In the step advancement logic (where steps transition from Pending to Running): if autonomy_level is Supervised, after queuing the task but before proceeding, set the goal run status to `AwaitingApproval` and emit an update with message "Supervised mode: awaiting operator acknowledgment for step: {step_title}". Reuse the existing AwaitingApproval status and approval flow (approval_type can be "autonomy_acknowledgment" to distinguish from policy approvals).
       - In `complete_goal_run`: set `authorship_tag` on the GoalRun before final persist. Call `classify_authorship(true, true)` -- all goal runs involve operator input (the goal text) and agent synthesis (the plan execution), so the default is `Joint`. If the goal was operator-initiated, tag as Joint. This aligns with the locked decision to track at goal output level.

    IMPORTANT: Default autonomy level is Aware per locked decision.
    IMPORTANT: Attribution is metadata, not inline commentary per AUTH-02.
    IMPORTANT: Supervised mode reuses existing AwaitingApproval mechanism per research recommendation.
    IMPORTANT: Aware mode is the current behavior -- no changes needed for Aware event emission except the explicit filtering gate.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo check -p tamux-daemon -p tamux-protocol 2>&1 | tail -20 && cargo test -p tamux-daemon --bin tamux-daemon 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AgentStartGoalRun IPC message carries autonomy_level
    - server.rs passes autonomy_level through to start_goal_run
    - emit_goal_run_update filters events based on autonomy level
    - Supervised mode sets AwaitingApproval at step boundaries
    - GoalRun gets authorship_tag on completion
    - `cargo check -p tamux-daemon -p tamux-protocol` compiles
    - All existing tests pass (zero regressions)
  </acceptance_criteria>
  <done>Autonomy dial wired end-to-end: IPC -> GoalRun -> event filtering -> acknowledgment gates (AUTO-01 through AUTO-04). Authorship tag set on goal completion (AUTH-01, AUTH-02). All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo check -p tamux-daemon -p tamux-protocol` compiles without errors
- `cargo test -p tamux-daemon autonomy` -- autonomy module tests pass
- `cargo test -p tamux-daemon authorship` -- authorship module tests pass
- `cargo test -p tamux-daemon --bin tamux-daemon` -- all existing tests pass (zero regressions)
- GoalRun round-trips with autonomy_level and authorship_tag via serde
- AgentStartGoalRun with autonomy_level round-trips via serde
</verification>

<success_criteria>
1. Operator can specify autonomy level (autonomous/aware/supervised) when starting a goal run
2. Autonomous mode suppresses intermediate events; operator sees only final report
3. Aware mode behaves as current default (milestones reported)
4. Supervised mode pauses at each step boundary for operator acknowledgment
5. Goal outputs carry authorship_tag metadata (operator/agent/joint)
6. Attribution is serde metadata, not inline text
7. All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-operator-control-transparency/04-02-SUMMARY.md`
</output>
