---
phase: 05-integration-wiring
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - crates/amux-daemon/src/agent/goal_llm.rs
  - crates/amux-daemon/src/agent/goal_planner.rs
  - crates/amux-daemon/src/agent/handoff/audit.rs
  - crates/amux-daemon/src/agent/handoff/broker.rs
autonomous: true
gap_closure: true
requirements: [UNCR-07, EPIS-08, HAND-04, HAND-05, COST-03, EMBD-01, EMBD-02, EMBD-03, EMBD-04]

must_haves:
  truths:
    - "Temperature is computed from real operator-message pacing and used in active confidence scoring."
    - "Specialist output validation uses broker-linked handoff_log_id, never a synthetic fallback."
    - "Failed specialist validation blocks acceptance and routes the step through failure/replan handling."
  artifacts:
    - path: "crates/amux-daemon/src/agent/goal_llm.rs"
      provides: "Production compute_temperature call and score impact in confidence pipeline"
      contains: "compute_temperature("
    - path: "crates/amux-daemon/src/agent/handoff/broker.rs"
      provides: "Handoff log binding/query helpers for real task->handoff linkage"
      contains: "to_task_id"
    - path: "crates/amux-daemon/src/agent/goal_planner.rs"
      provides: "Validation gate before step acceptance in completion path"
      contains: "validate_specialist_output"
  key_links:
    - from: "crates/amux-daemon/src/agent/goal_llm.rs"
      to: "crates/amux-daemon/src/agent/embodied/dimensions.rs"
      via: "compute_temperature(recent_message_count, avg_gap_secs)"
      pattern: "compute_temperature\\("
    - from: "crates/amux-daemon/src/agent/handoff/broker.rs"
      to: "handoff_log.to_task_id"
      via: "bind persisted handoff_log_id to spawned task_id"
      pattern: "UPDATE handoff_log SET to_task_id"
    - from: "crates/amux-daemon/src/agent/goal_planner.rs"
      to: "handle_goal_run_step_failure"
      via: "validation failure fail-closed routing"
      pattern: "handle_goal_run_step_failure\\("
---

<objective>
Close the two remaining Phase 05 verification gaps by wiring temperature into an active scoring path and enforcing real-ID specialist validation gating.

Purpose: Finish EMBD-02 and HAND-05 at production quality so verification can pass without synthetic linkage or bypass behavior.
Output: Runtime/planning confidence path consumes temperature; handoff validation resolves real handoff_log_id and blocks acceptance when validation fails.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-wiring/05-VERIFICATION.md
@.planning/phases/05-integration-wiring/05-01-SUMMARY.md
@.planning/phases/05-integration-wiring/05-02-SUMMARY.md
</context>

<interfaces>
From crates/amux-daemon/src/agent/embodied/dimensions.rs:
```rust
pub fn compute_temperature(recent_message_count: u32, avg_gap_secs: u64) -> f64;
```

From crates/amux-daemon/src/agent/types.rs:
```rust
pub struct AgentMessage { pub role: MessageRole, pub timestamp: u64, ... }
pub enum MessageRole { System, User, Assistant, Tool }
```

From crates/amux-daemon/src/agent/handoff/broker.rs:
```rust
pub(crate) async fn route_handoff(...) -> Result<HandoffResult>;
pub(crate) async fn validate_specialist_output(
    &self,
    handoff_log_id: &str,
    task_id: &str,
    acceptance_criteria: &AcceptanceCriteria,
) -> Result<ValidationResult>;
```

From crates/amux-daemon/src/agent/handoff/schema.rs:
```sql
handoff_log(id TEXT PRIMARY KEY, to_task_id TEXT, outcome TEXT, ...)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire EMBD-02 temperature into active confidence scoring path</name>
  <files>crates/amux-daemon/src/agent/goal_llm.rs</files>
  <read_first>
    - crates/amux-daemon/src/agent/goal_llm.rs (annotate_plan_steps_with_confidence, request_goal_plan caller)
    - crates/amux-daemon/src/agent/types.rs (AgentMessage + MessageRole fields)
    - crates/amux-daemon/src/agent/embodied/dimensions.rs (compute_temperature contract)
  </read_first>
  <action>
    Replace the current "temperature is runtime only" comment-only behavior by computing a real temperature signal in `annotate_plan_steps_with_confidence` and applying it to scoring (per EMBD-02 gap).

    Implementation requirements:
    1) Pass thread context into `annotate_plan_steps_with_confidence` from `request_goal_plan` (use `goal_run.thread_id.as_deref()`).
    2) Derive temperature inputs from actual thread messages:
       - `recent_message_count`: count `MessageRole::User` messages in the last 5 minutes.
       - `avg_gap_secs`: average timestamp gap between the last up-to-5 user messages (0 when insufficient data).
    3) Call `compute_temperature(recent_message_count, avg_gap_secs)` in this active path.
    4) Feed computed temperature into confidence scoring (e.g., blend into blast-radius/risk score with clamp to [0.0, 1.0]) so it changes final confidence behavior rather than dead-code computation.
    5) Keep existing calibration flow intact (`get_calibrated_band` still runs after confidence assessment).
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p tamux-daemon --lib embodied::dimensions -- --nocapture && cargo check -p tamux-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `goal_llm.rs` contains a production `compute_temperature(` call in `annotate_plan_steps_with_confidence`.
    - Temperature value is incorporated into the scoring path before `compute_step_confidence`.
    - `cargo check -p tamux-daemon` passes.
  </acceptance_criteria>
  <done>EMBD-02 gap is closed: temperature is computed from real operator pacing data and influences active confidence scoring.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce HAND-05 validation gate with real handoff_log linkage</name>
  <files>crates/amux-daemon/src/agent/handoff/audit.rs, crates/amux-daemon/src/agent/handoff/broker.rs, crates/amux-daemon/src/agent/goal_planner.rs</files>
  <read_first>
    - crates/amux-daemon/src/agent/handoff/audit.rs (log_handoff_detail / update_handoff_outcome DB helpers)
    - crates/amux-daemon/src/agent/handoff/broker.rs (route_handoff + validate_specialist_output)
    - crates/amux-daemon/src/agent/goal_planner.rs (enqueue_goal_run_step + handle_goal_run_step_completion)
    - .planning/phases/05-integration-wiring/05-VERIFICATION.md (HAND-05 reason + missing items)
  </read_first>
  <action>
    Remove synthetic handoff IDs and make validation fail-closed (per HAND-05 gap).

    Implementation requirements:
    1) In handoff persistence helpers, add methods to:
       - bind `to_task_id` for a known `handoff_log_id` immediately after task creation in `route_handoff`;
       - resolve `handoff_log_id` from `to_task_id` for completion-time validation.
    2) Update `route_handoff` so dispatched specialist tasks are actually linked in SQLite (`handoff_log.to_task_id = task.id`).
    3) In `handle_goal_run_step_completion`, perform specialist validation gate **before** final step-acceptance mutation:
       - resolve real `handoff_log_id` from persisted linkage;
       - call `validate_specialist_output` with resolved id and step criteria;
       - if validation fails (or linkage/validation errors), fail closed by routing through `handle_goal_run_step_failure` with explicit reason and return early;
       - only mark step completed and advance index when validation passes.
    4) Remove synthetic fallback logic (`format!("hlid_{}", task.id)`).
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p tamux-daemon --lib handoff::acceptance -- --nocapture && cargo test -p tamux-daemon --lib handoff::escalation -- --nocapture && cargo check -p tamux-daemon</automated>
  </verify>
  <acceptance_criteria>
    - No synthetic `hlid_` fallback remains in `goal_planner.rs`.
    - `route_handoff` persists real task-to-handoff linkage (`to_task_id`) before completion.
    - Completion path blocks acceptance when specialist validation fails and executes failure/replan path instead.
    - `cargo check -p tamux-daemon` passes.
  </acceptance_criteria>
  <done>HAND-05 quality gap is closed: specialist output validation is linked to real handoff logs and enforced as an acceptance gate.</done>
</task>

</tasks>

<verification>
- `cargo check -p tamux-daemon`
- `cargo test -p tamux-daemon --lib handoff::acceptance -- --nocapture`
- `cargo test -p tamux-daemon --lib handoff::escalation -- --nocapture`
- `cargo test -p tamux-daemon --lib embodied::dimensions -- --nocapture`
- `rg -n "compute_temperature\\(" crates/amux-daemon/src/agent/goal_llm.rs`
- `rg -n "hlid_" crates/amux-daemon/src/agent/goal_planner.rs` (must return no matches)
</verification>

<success_criteria>
1. EMBD-02: `compute_temperature` is called in an active scoring path and affects confidence computation.
2. HAND-05: `validate_specialist_output` uses broker-linked real handoff_log_id (no synthetic ID).
3. HAND-05: validation failures prevent step acceptance and trigger failure/replan handling.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-wiring/05-03-SUMMARY.md`
</output>
