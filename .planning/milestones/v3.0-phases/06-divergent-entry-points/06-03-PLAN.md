---
phase: 06-divergent-entry-points
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - crates/amux-daemon/src/agent/handoff/divergent.rs
  - crates/amux-daemon/src/agent/dispatcher.rs
  - crates/amux-daemon/src/agent/tool_executor.rs
  - crates/amux-protocol/src/messages.rs
  - crates/amux-daemon/src/server.rs
autonomous: true
requirements: [DIVR-02, DIVR-03]
gap_closure: true
must_haves:
  truths:
    - "When divergent framing tasks finish, their outputs are recorded as divergent contributions."
    - "When all framings contribute, the divergent session produces operator-facing tensions plus a mediator prompt."
    - "Tool and IPC operator flows can fetch completed divergent outputs (not only session-start confirmations)."
  artifacts:
    - path: "crates/amux-daemon/src/agent/handoff/divergent.rs"
      provides: "Task-to-session contribution recording and completion payload assembly"
      contains: "record_divergent_contribution"
    - path: "crates/amux-daemon/src/agent/dispatcher.rs"
      provides: "Runtime hook calling divergent contribution/completion logic on divergent task completion"
      contains: "updated.source == \"divergent\""
    - path: "crates/amux-daemon/src/agent/tool_executor.rs"
      provides: "Operator retrievable divergent output via tool path"
      contains: "get_divergent_session"
    - path: "crates/amux-protocol/src/messages.rs"
      provides: "IPC request/response for divergent output retrieval"
      contains: "AgentGetDivergentSession"
    - path: "crates/amux-daemon/src/server.rs"
      provides: "IPC handler returning completed tensions/mediator payload"
      contains: "AgentGetDivergentSession"
  key_links:
    - from: "crates/amux-daemon/src/agent/dispatcher.rs"
      to: "crates/amux-daemon/src/agent/handoff/divergent.rs"
      via: "post-completion hook for source=divergent tasks"
      pattern: "record_divergent_contribution"
    - from: "crates/amux-daemon/src/agent/handoff/divergent.rs"
      to: "crates/amux-daemon/src/agent/tool_executor.rs"
      via: "session output getter used by get_divergent_session tool"
      pattern: "get_divergent_session"
    - from: "crates/amux-daemon/src/server.rs"
      to: "crates/amux-daemon/src/agent/handoff/divergent.rs"
      via: "AgentGetDivergentSession IPC query path"
      pattern: "AgentGetDivergentSession"
---

<objective>
Close Phase 06 verification gaps by wiring divergent lifecycle completion end-to-end: record framing contributions at runtime, complete sessions when all framings finish, and expose tensions/mediator output to operators via tool and IPC.

Purpose: Phase 06 currently starts divergent sessions but only returns start confirmations. This plan closes the missing operator-facing value path required by DIVR-02 and DIVR-03.
Output: Divergent completion wiring, retrievable tension payloads, and tests proving contribution recording + surfaced mediator output.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-divergent-entry-points/06-VERIFICATION.md
@.planning/phases/06-divergent-entry-points/06-01-SUMMARY.md
@.planning/phases/06-divergent-entry-points/06-02-SUMMARY.md
@crates/amux-daemon/src/agent/handoff/divergent.rs
@crates/amux-daemon/src/agent/dispatcher.rs
@crates/amux-daemon/src/agent/tool_executor.rs
@crates/amux-protocol/src/messages.rs
@crates/amux-daemon/src/server.rs

<interfaces>
From crates/amux-daemon/src/agent/handoff/divergent.rs:
```rust
pub(crate) async fn start_divergent_session(
    &self,
    problem_statement: &str,
    custom_framings: Option<Vec<Framing>>,
    thread_id: &str,
    goal_run_id: Option<&str>,
) -> Result<String>;

pub(crate) async fn record_divergent_contribution(
    &self,
    session_id: &str,
    framing_label: &str,
    content: &str,
) -> Result<()>;

pub(crate) async fn complete_divergent_session(&self, session_id: &str) -> Result<String>;
```

From crates/amux-daemon/src/agent/dispatcher.rs (task completion path):
```rust
if let Some(current) = tasks.iter_mut().find(|entry| entry.id == task.id) {
    current.status = if waiting_for_subagents {
        TaskStatus::Blocked
    } else {
        TaskStatus::Completed
    };
    // ...
}
```

From crates/amux-daemon/src/agent/tool_executor.rs:
```rust
"run_divergent" => execute_run_divergent(&args, agent, thread_id, task_id).await
```

From crates/amux-protocol/src/messages.rs and crates/amux-daemon/src/server.rs:
```rust
ClientMessage::AgentStartDivergentSession { ... }
DaemonMessage::AgentDivergentSessionStarted { session_json: String }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire divergent contribution recording and session completion into runtime task completion</name>
  <files>crates/amux-daemon/src/agent/handoff/divergent.rs, crates/amux-daemon/src/agent/dispatcher.rs</files>
  <behavior>
    - Test 1: Completing a divergent framing task records one contribution linked to the correct framing/session.
    - Test 2: When the final framing contribution is recorded, session completion generates tensions and a mediator prompt.
    - Test 3: Non-divergent tasks do not trigger divergent contribution logic.
  </behavior>
  <action>Implement the missing verification gap wiring by adding a dispatcher completion hook (for `updated.source == "divergent"` and `TaskStatus::Completed`) that calls new helper logic in `handoff/divergent.rs` to: (a) resolve session+framing by completed task id, (b) record contribution text from task completion summary/content, and (c) call `complete_divergent_session` exactly once when all framings have contributed. Persist completion artifacts (including tensions markdown and mediator prompt) on session state so operator surfaces can return them. Keep existing behavior unchanged for non-divergent tasks. Addresses verification gap: “Wire task completion/contribution events into record_divergent_contribution.”</action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p amux-daemon divergent -- --nocapture</automated>
  </verify>
  <done>Divergent framing task completion now records contributions and auto-completes sessions with tensions+mediator data once all framings finish.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface completed divergent tensions/mediator output through tool and IPC retrieval paths</name>
  <files>crates/amux-daemon/src/agent/handoff/divergent.rs, crates/amux-daemon/src/agent/tool_executor.rs, crates/amux-protocol/src/messages.rs, crates/amux-daemon/src/server.rs</files>
  <behavior>
    - Test 1: Tool query for a completed divergent session returns status plus tensions and mediator prompt fields.
    - Test 2: IPC `AgentGetDivergentSession` returns the same completed payload schema as tool path.
    - Test 3: In-progress sessions return progress metadata without falsely reporting completion output.
  </behavior>
  <action>Add a single canonical AgentEngine getter for divergent session output payload (status, framing progress, tensions, mediator prompt, optional mediation result). Expose it in two operator paths: (1) a new tool action `get_divergent_session` in `tool_executor.rs`, and (2) new IPC request/response variants (`AgentGetDivergentSession` + response) wired in `server.rs`. Keep `run_divergent` start behavior intact but include explicit follow-up guidance to retrieve session output. Addresses verification gaps: “Add a completion path ... exposes formatted tensions/mediator output” and “Return or stream disagreement/tension payloads from tool and/or IPC flow.”</action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo check -p amux-protocol -p amux-daemon && cargo test -p amux-daemon divergent -- --nocapture</automated>
  </verify>
  <done>Operators can retrieve completed divergent tensions and mediator output via both tool and IPC, not just start confirmations.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for end-to-end divergent lifecycle gap closure claims</name>
  <files>crates/amux-daemon/src/agent/handoff/divergent.rs, crates/amux-daemon/src/agent/tool_executor.rs, crates/amux-daemon/src/server.rs, crates/amux-protocol/src/messages.rs</files>
  <action>Add focused regression tests that prove the exact failed truth in 06-VERIFICATION is now true: a session started from entry points reaches contribution recording, completion, and operator-visible tensions/mediator payload. Include one daemon-side flow test (task completion -> session output), one tool serialization test, and one protocol message serde round-trip for the new retrieval message pair. Keep test names explicit with `divergent_` prefix for discoverability in future verification reruns.</action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p amux-protocol && cargo test -p amux-daemon -- divergent --nocapture</automated>
  </verify>
  <done>Automated tests fail if contribution recording, completion synthesis, or operator-facing divergent output retrieval regresses.</done>
</task>

</tasks>

<verification>
1. `cargo check -p amux-protocol -p amux-daemon` succeeds.
2. `cargo test -p amux-daemon -- divergent --nocapture` passes with new lifecycle coverage.
3. `cargo test -p amux-protocol` passes with retrieval message serde coverage.
4. `grep -n "record_divergent_contribution" crates/amux-daemon/src/agent/dispatcher.rs` shows runtime completion hook.
5. `grep -n "AgentGetDivergentSession" crates/amux-protocol/src/messages.rs crates/amux-daemon/src/server.rs` shows IPC retrieval wiring.
6. `grep -n "get_divergent_session" crates/amux-daemon/src/agent/tool_executor.rs` shows tool retrieval path.
</verification>

<success_criteria>
- DIVR-02: Operator-facing tensions are surfaced through tool/IPC retrieval payloads after divergent completion.
- DIVR-03: Mediator synthesis prompt/output is produced on session completion and returned in operator-facing payloads.
- Phase 06 verification gap (“only session start confirmations returned”) is eliminated with automated evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-divergent-entry-points/06-03-SUMMARY.md`
</output>
