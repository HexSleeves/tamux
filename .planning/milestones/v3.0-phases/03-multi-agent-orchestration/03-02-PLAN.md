---
phase: 03-multi-agent-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/amux-daemon/src/agent/handoff/context_bundle.rs
  - crates/amux-daemon/src/agent/handoff/escalation.rs
  - crates/amux-daemon/src/agent/handoff/acceptance.rs
  - crates/amux-daemon/src/agent/handoff/audit.rs
  - crates/amux-daemon/src/agent/handoff/mod.rs
autonomous: true
requirements: [HAND-02, HAND-03, HAND-04, HAND-05, HAND-06, HAND-08]

must_haves:
  truths:
    - "Context bundles are assembled from episodic refs, negative constraints, partial outputs, and parent context with a strict 2000-token ceiling"
    - "Bundle estimated_tokens is enforced by summarizing the largest fields first when over ceiling"
    - "Handoff depth is tracked in the bundle and handoffs are refused at depth >= 3"
    - "Escalation triggers (ConfidenceBelow, ToolFails, TimeExceeds) evaluate correctly against runtime state"
    - "Acceptance criteria validation checks structural conditions before optional LLM validation"
    - "Every handoff event is recorded to the WORM audit trail via record_telemetry_event"
    - "Handoff log entries persist full context bundle and acceptance criteria in SQLite"
  artifacts:
    - path: "crates/amux-daemon/src/agent/handoff/context_bundle.rs"
      provides: "ContextBundle assembly, token estimation, enforce_token_ceiling, depth checking"
      min_lines: 80
    - path: "crates/amux-daemon/src/agent/handoff/escalation.rs"
      provides: "evaluate_escalation_triggers, confidence_band_order helper"
      min_lines: 40
    - path: "crates/amux-daemon/src/agent/handoff/acceptance.rs"
      provides: "AcceptanceCriteria validation, structural checks"
      min_lines: 40
    - path: "crates/amux-daemon/src/agent/handoff/audit.rs"
      provides: "record_handoff_audit (WORM), log_handoff_detail (SQLite)"
      min_lines: 50
  key_links:
    - from: "crates/amux-daemon/src/agent/handoff/context_bundle.rs"
      to: "crates/amux-daemon/src/agent/episodic/retrieval.rs"
      via: "EpisodeRef struct references episodes from retrieval"
      pattern: "EpisodeRef"
    - from: "crates/amux-daemon/src/agent/handoff/audit.rs"
      to: "crates/amux-daemon/src/history.rs"
      via: "record_telemetry_event with kind='handoff'"
      pattern: "record_telemetry_event.*handoff"
    - from: "crates/amux-daemon/src/agent/handoff/escalation.rs"
      to: "crates/amux-daemon/src/agent/explanation.rs"
      via: "ConfidenceBand ordering for ConfidenceBelow trigger"
      pattern: "confidence_band_order"
---

<objective>
Implement the core handoff business logic: context bundle assembly with token ceiling enforcement, escalation trigger evaluation, acceptance criteria validation, and WORM audit trail recording. These are the policy and data-flow components that the broker routing (Plan 03) will orchestrate.

Purpose: Provide the intelligence layer -- deciding what context to send, when to escalate, how to validate output, and how to audit -- that makes handoffs reliable rather than just mechanical task dispatch.
Output: 4 new files (context_bundle.rs, escalation.rs, acceptance.rs, audit.rs) with full logic and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-agent-orchestration/03-RESEARCH.md
@.planning/phases/03-multi-agent-orchestration/03-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan implements against -->

From crates/amux-daemon/src/agent/handoff/mod.rs (created in Plan 01):
```rust
pub struct ContextBundle {
    pub task_spec: String,
    pub acceptance_criteria: String,
    pub episodic_refs: Vec<EpisodeRef>,
    pub negative_constraints: Vec<String>,
    pub partial_outputs: Vec<PartialOutput>,
    pub parent_context_summary: String,
    pub handoff_depth: u8,
    pub estimated_tokens: u32,
}

pub struct EpisodeRef { pub episode_id: String, pub summary: String, pub outcome: String }
pub struct PartialOutput { pub step_index: usize, pub content: String, pub status: String }
pub struct AcceptanceCriteria { pub description: String, pub structural_checks: Vec<String>, pub require_llm_validation: bool }

pub enum HandoffEscalationTrigger {
    ConfidenceBelow(String), ToolFails(u32), TimeExceeds(u64),
}
pub enum HandoffEscalationAction {
    HandBack, RetryWithNewContext, EscalateTo(String), AbortWithReport,
}
pub struct HandoffEscalationRule { pub trigger: HandoffEscalationTrigger, pub action: HandoffEscalationAction }
```

From crates/amux-daemon/src/agent/explanation.rs:
```rust
pub enum ConfidenceBand { Confident, Likely, Uncertain, Guessing }
impl ConfidenceBand { pub fn as_str(&self) -> &str { ... } }
```

From crates/amux-daemon/src/agent/episodic/retrieval.rs:
```rust
impl AgentEngine {
    pub(crate) async fn retrieve_relevant_episodes(&self, query: &str, limit: usize) -> Result<Vec<Episode>>
}
```

From crates/amux-daemon/src/agent/episodic/negative_knowledge.rs:
```rust
impl AgentEngine {
    pub(crate) async fn query_active_constraints(&self, entity_filter: Option<&str>) -> Result<Vec<NegativeConstraint>>
}
```

From crates/amux-daemon/src/agent/goal_parsing.rs:
```rust
pub(in crate::agent) fn summarize_text(value: &str, max_chars: usize) -> String
```

From crates/amux-daemon/src/history.rs:
```rust
// WORM audit: self.history.record_telemetry_event("handoff", &payload).await
// Detailed log: self.history.conn.call(|conn| { INSERT INTO handoff_log ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Context bundle assembly, token ceiling, and escalation trigger evaluation</name>
  <files>
    crates/amux-daemon/src/agent/handoff/context_bundle.rs,
    crates/amux-daemon/src/agent/handoff/escalation.rs,
    crates/amux-daemon/src/agent/handoff/mod.rs
  </files>
  <read_first>
    crates/amux-daemon/src/agent/handoff/mod.rs (full file -- Plan 01 types),
    crates/amux-daemon/src/agent/goal_parsing.rs (lines 166-180 for summarize_text),
    crates/amux-daemon/src/agent/explanation.rs (lines 22-40 for ConfidenceBand),
    crates/amux-daemon/src/agent/subagent/context_budget.rs (first 50 lines for budget pattern)
  </read_first>
  <behavior>
    Context bundle tests:
    - Test: new bundle with small fields stays under ceiling (estimated_tokens < 2000)
    - Test: bundle with large parent_context_summary gets summarized to fit ceiling
    - Test: bundle with large partial_outputs gets partial_outputs trimmed after parent_context
    - Test: estimated_tokens uses chars/4 approximation
    - Test: enforce_token_ceiling(2000) reduces estimated_tokens to <= 2000
    - Test: handoff_depth starts at 0 for first handoff
    - Test: is_at_depth_limit returns true when handoff_depth >= 3 (HAND-08)
    - Test: increment_depth increases by 1

    Escalation trigger tests:
    - Test: ConfidenceBelow("low") fires when confidence is "guessing" (lower than "low")
    - Test: ConfidenceBelow("medium") fires when confidence is "low"
    - Test: ConfidenceBelow("medium") does NOT fire when confidence is "high"
    - Test: ToolFails(3) fires when consecutive_failures >= 3
    - Test: ToolFails(3) does NOT fire when consecutive_failures == 2
    - Test: TimeExceeds(300) fires when elapsed_secs >= 300
    - Test: evaluate_escalation_triggers returns first matching trigger's action
    - Test: evaluate_escalation_triggers returns None when no triggers fire
  </behavior>
  <action>
    Create `crates/amux-daemon/src/agent/handoff/context_bundle.rs`:

    1. Add `impl ContextBundle` block with:
       - `pub fn new(task_spec: String, acceptance_criteria: String) -> Self` -- creates empty bundle with depth 0
       - `pub fn estimate_tokens(text: &str) -> u32` -- chars / 4 approximation (same as APPROX_CHARS_PER_TOKEN used elsewhere in codebase)
       - `pub fn recompute_estimated_tokens(&mut self)` -- sums tokens across all fields
       - `pub fn enforce_token_ceiling(&mut self, max_tokens: u32)` -- while over ceiling: first summarize parent_context_summary (use `super::super::goal_parsing::summarize_text` with progressively smaller char limits), then trim partial_outputs (remove oldest first), then truncate negative_constraints. Recompute after each step.
       - `pub fn is_at_depth_limit(&self) -> bool` -- `self.handoff_depth >= 3` per HAND-08
       - `pub fn increment_depth(&mut self)` -- `self.handoff_depth += 1`

    2. Add `impl Default for ContextBundle` -- all empty, depth 0, tokens 0.

    Create `crates/amux-daemon/src/agent/handoff/escalation.rs`:

    1. `pub fn confidence_band_order(band: &str) -> u8` -- maps band name strings to ordering: "guessing"->0, "uncertain"/"low"->1, "likely"/"medium"->2, "confident"/"high"->3. Case-insensitive. Unknown defaults to 0.

    2. `pub fn evaluate_escalation_triggers(rules: &[HandoffEscalationRule], consecutive_failures: u32, elapsed_secs: u64, confidence_band: &str) -> Option<HandoffEscalationAction>` -- iterates rules, checks each trigger, returns first matching action (clone). Per locked decisions: ConfidenceBelow checks current < threshold, ToolFails checks >= threshold, TimeExceeds checks >= threshold.

    Update `crates/amux-daemon/src/agent/handoff/mod.rs`:
    - Add `pub mod context_bundle;` and `pub mod escalation;` declarations
    - Re-export key functions

    Write inline `#[cfg(test)] mod tests` in each file. Use `make_` prefix for test helpers per convention.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p tamux-daemon handoff::context_bundle handoff::escalation -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ContextBundle enforces 2000-token ceiling by summarizing largest fields first
    - Depth limit at 3 hops per HAND-08
    - Token estimation uses chars/4
    - Escalation triggers evaluate correctly for all 3 trigger types
    - confidence_band_order handles both internal names (guessing/uncertain/likely/confident) and user-facing names (low/medium/high)
    - 16+ unit tests pass
    - cargo check clean
  </acceptance_criteria>
  <done>Context bundle with token ceiling enforcement and escalation trigger evaluation implemented with all tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Acceptance criteria validation and WORM handoff audit trail</name>
  <files>
    crates/amux-daemon/src/agent/handoff/acceptance.rs,
    crates/amux-daemon/src/agent/handoff/audit.rs,
    crates/amux-daemon/src/agent/handoff/mod.rs
  </files>
  <read_first>
    crates/amux-daemon/src/agent/handoff/mod.rs (full file -- types + Task 1 additions),
    crates/amux-daemon/src/agent/handoff/schema.rs (full file -- table definitions),
    crates/amux-daemon/src/agent/episodic/store.rs (lines 1-50 for WORM append pattern),
    crates/amux-daemon/src/history.rs (search for "record_telemetry_event" for WORM pattern, search for "worm_chain_tip" for chain tip pattern)
  </read_first>
  <behavior>
    Acceptance validation tests:
    - Test: validate with empty output returns failure ("output is empty")
    - Test: validate with non-empty output and no structural_checks returns pass
    - Test: structural check "non_empty" passes when output is non-empty
    - Test: structural check "min_length:100" fails when output is 50 chars
    - Test: structural check "min_length:100" passes when output is 150 chars
    - Test: structural check "contains:error" fails when output doesn't contain "error"
    - Test: structural check "contains:success" passes when output contains "success"
    - Test: multiple structural checks -- all must pass for overall pass
    - Test: require_llm_validation flag is reported in result (but LLM call deferred to broker)

    Audit tests:
    - Test: format_handoff_audit_payload creates correct JSON structure with all required fields
    - Test: format_handoff_audit_payload includes handoff_log_id reference
    - Test: log_handoff_detail produces correct INSERT parameters
  </behavior>
  <action>
    Create `crates/amux-daemon/src/agent/handoff/acceptance.rs`:

    1. `pub struct ValidationResult { pub passed: bool, pub failures: Vec<String>, pub needs_llm_validation: bool }`

    2. `impl AcceptanceCriteria`:
       - `pub fn validate_structural(&self, output: &str) -> ValidationResult` -- checks: (a) output is non-empty, (b) for each structural_check: parse "non_empty", "min_length:N", "contains:TEXT", "matches_regex:PATTERN". Return failures for each failed check. Set needs_llm_validation from self.require_llm_validation only if structural checks pass.
       - `pub fn default_for_code() -> Self` -- structural_checks: ["non_empty", "min_length:50"], require_llm_validation: true
       - `pub fn default_for_research() -> Self` -- structural_checks: ["non_empty", "min_length:100"], require_llm_validation: true
       - `pub fn default_for_review() -> Self` -- structural_checks: ["non_empty"], require_llm_validation: false (review output is inherently subjective)

    Create `crates/amux-daemon/src/agent/handoff/audit.rs`:

    1. `pub fn format_handoff_audit_payload(from_task_id: &str, to_specialist: &str, to_task_id: &str, task_description: &str, outcome: &str, duration_ms: Option<u64>, confidence_band: Option<&str>, handoff_log_id: &str) -> serde_json::Value` -- creates the JSON payload for WORM audit per HAND-06 locked decision (from, to, task, outcome, duration, confidence, audit_hash).

    2. `impl AgentEngine` block in audit.rs:
       - `pub(super) async fn record_handoff_audit(&self, ...) -> Result<()>` -- calls `self.history.record_telemetry_event("handoff", &payload).await` using the formatted payload. This is the WORM entry.
       - `pub(super) async fn log_handoff_detail(&self, handoff_log_id: &str, from_task_id: &str, to_specialist_id: &str, to_task_id: Option<&str>, task_description: &str, acceptance_criteria_json: &str, context_bundle_json: &str, handoff_depth: u8, outcome: &str, confidence_band: Option<&str>) -> Result<()>` -- INSERT into handoff_log table (detailed record that WORM entry references). Uses `self.history.conn.call()` pattern.
       - `pub(super) async fn update_handoff_outcome(&self, handoff_log_id: &str, outcome: &str, duration_ms: Option<u64>, error_message: Option<&str>) -> Result<()>` -- UPDATE handoff_log SET outcome, duration_ms, completed_at, error_message.

    Update `crates/amux-daemon/src/agent/handoff/mod.rs`:
    - Add `pub mod acceptance;` and `pub mod audit;` declarations
    - Re-export ValidationResult

    Write inline tests. Note: audit.rs `impl AgentEngine` methods will be tested via integration in Plan 03 since they need a real HistoryStore. Unit tests here focus on `format_handoff_audit_payload` (pure function) and `validate_structural` (pure function).
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p tamux-daemon handoff::acceptance handoff::audit -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - AcceptanceCriteria validates structural checks (non_empty, min_length, contains)
    - ValidationResult reports all failures, not just first
    - needs_llm_validation only set when structural checks pass
    - format_handoff_audit_payload produces correct JSON with all HAND-06 fields
    - audit.rs has AgentEngine methods for WORM recording and SQLite logging
    - 12+ unit tests pass
    - cargo check -p tamux-daemon clean
  </acceptance_criteria>
  <done>Acceptance validation with structural checks and WORM audit trail recording implemented, all tests passing</done>
</task>

</tasks>

<verification>
1. `cargo check -p tamux-daemon` compiles without errors
2. `cargo test -p tamux-daemon handoff` -- all handoff tests pass (Plan 01 + Plan 02)
3. `cargo test -p tamux-daemon --bin tamux-daemon` -- all existing tests still pass
4. Context bundle enforces 2000-token ceiling
5. Escalation triggers evaluate correctly
6. Acceptance criteria structural checks work
7. Audit payload matches HAND-06 spec
</verification>

<success_criteria>
- 4 new files: context_bundle.rs, escalation.rs, acceptance.rs, audit.rs
- Context bundle assembly with chars/4 token estimation and ceiling enforcement
- Escalation trigger evaluation for ConfidenceBelow, ToolFails, TimeExceeds
- Acceptance validation with 3 structural check types
- WORM audit trail via record_telemetry_event("handoff", ...)
- Detailed handoff_log SQLite persistence
- Depth limit enforcement at 3 hops
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-agent-orchestration/03-02-SUMMARY.md`
</output>
