---
phase: 08-client-explainability-divergent-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/agent-chat-panel/runtime.tsx
autonomous: true
requirements: [DIVR-02, EXPL-01, EXPL-02, EXPL-03]
must_haves:
  truths:
    - "Frontend `!explain` renders explainability payloads to the active thread."
    - "Frontend `!diverge` and `!diverge-get` render divergent payloads even when Electron resolves query responses without forwarding as `agent-event`."
    - "Existing daemon event path remains backward-compatible for explain/divergent payload display."
  artifacts:
    - path: "frontend/src/components/agent-chat-panel/runtime.tsx"
      provides: "Unified payload handling for invoke responses and daemon events"
      contains: "appendDaemonSystemMessage"
  key_links:
    - from: "frontend/electron/main.cjs"
      to: "frontend/src/components/agent-chat-panel/runtime.tsx"
      via: "IPC invoke response payload handling in command path"
      pattern: "agent-explain-action|agent-start-divergent-session|agent-get-divergent-session"
---

<objective>
Fix frontend operator-facing explain/divergent E2E rendering gaps by handling invoke response payloads directly in the runtime command path, while preserving existing daemon-event rendering behavior.
</objective>

<tasks>
<task type="auto">
  <name>Handle explain/divergent invoke response payloads directly in runtime command flow</name>
  <files>frontend/src/components/agent-chat-panel/runtime.tsx</files>
  <action>
    Add shared payload/message helpers, normalize bridge payload shape, and render successful or error responses for `!explain`, `!diverge`, and `!diverge-get` directly from await-ed IPC invoke returns.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo check --workspace</automated>
    <automated>cd /home/mkurman/gitlab/it/cmux-next/frontend && npm run build</automated>
  </verify>
  <done>Frontend command UX reliably displays explain/divergent output regardless of bridge event-forwarding behavior.</done>
</task>

<task type="auto">
  <name>Preserve compatibility with daemon event-based payload rendering</name>
  <files>frontend/src/components/agent-chat-panel/runtime.tsx</files>
  <action>
    Reuse the same helper path in `onAgentEvent` handling for `agent-explanation`, `agent-divergent-session-started`, and `agent-divergent-session` to avoid behavioral drift between event and query-response paths.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && rg -n "agent-explanation|agent-divergent-session-started|agent-divergent-session|appendDaemonSystemMessage|normalizeBridgePayload" frontend/src/components/agent-chat-panel/runtime.tsx</automated>
  </verify>
  <done>Both direct response and event-stream paths render consistent operator-visible payloads.</done>
</task>
</tasks>
