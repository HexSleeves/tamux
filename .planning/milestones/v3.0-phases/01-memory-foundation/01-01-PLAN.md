---
phase: 01-memory-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amux-daemon/src/agent/episodic/mod.rs
  - crates/amux-daemon/src/agent/episodic/schema.rs
  - crates/amux-daemon/src/agent/episodic/store.rs
  - crates/amux-daemon/src/agent/episodic/privacy.rs
  - crates/amux-daemon/src/agent/episodic/links.rs
  - crates/amux-daemon/src/agent/mod.rs
  - crates/amux-daemon/src/agent/engine.rs
  - crates/amux-daemon/src/agent/types.rs
  - crates/amux-daemon/src/history.rs
  - crates/amux-daemon/Cargo.toml
autonomous: true
requirements: [EPIS-01, EPIS-07, EPIS-08, EPIS-09, EPIS-10, EPIS-11]

must_haves:
  truths:
    - "Episode records can be inserted into SQLite and read back with all fields intact"
    - "Episode links (retry_of, builds_on, contradicts, supersedes, caused_by) can be created between episodes"
    - "PII is scrubbed from episode summaries and root_cause fields before storage"
    - "Episodes with expired TTL are excluded from queries"
    - "Episode writes are also appended to a WORM 'episodic' ledger"
    - "EpisodicConfig defaults are applied when no config is present"
    - "The daemon compiles and all existing tests pass"
  artifacts:
    - path: "crates/amux-daemon/src/agent/episodic/mod.rs"
      provides: "All episodic data types (Episode, EpisodeType, EpisodeOutcome, CausalStep, EpisodeLink, LinkType, NegativeConstraint, ConstraintType, CounterWhoState, TriedApproach, CorrectionPattern, EpisodicStore, EpisodicConfig)"
      exports: ["Episode", "EpisodeType", "EpisodeOutcome", "CausalStep", "EpisodeLink", "LinkType", "NegativeConstraint", "ConstraintType", "CounterWhoState", "TriedApproach", "CorrectionPattern", "EpisodicStore", "EpisodicConfig"]
    - path: "crates/amux-daemon/src/agent/episodic/schema.rs"
      provides: "SQLite CREATE TABLE statements for episodes, episodes_fts, episode_links, negative_knowledge, counter_who_state; FTS5 sync triggers; indexes"
      contains: "CREATE TABLE IF NOT EXISTS episodes"
    - path: "crates/amux-daemon/src/agent/episodic/store.rs"
      provides: "impl AgentEngine methods for insert_episode, get_episode, list_episodes_for_goal_run"
      contains: "async fn record_episode"
    - path: "crates/amux-daemon/src/agent/episodic/privacy.rs"
      provides: "scrub_episode wrapper, TTL enforcement, session suppression check"
      contains: "fn scrub_episode"
    - path: "crates/amux-daemon/src/agent/episodic/links.rs"
      provides: "Episode link creation and querying"
      contains: "fn create_episode_link"
  key_links:
    - from: "crates/amux-daemon/src/agent/engine.rs"
      to: "crates/amux-daemon/src/agent/episodic/mod.rs"
      via: "episodic_store: RwLock<EpisodicStore> field on AgentEngine"
      pattern: "episodic_store.*RwLock"
    - from: "crates/amux-daemon/src/agent/episodic/store.rs"
      to: "crates/amux-daemon/src/history.rs"
      via: "self.history.conn.call() for SQLite writes"
      pattern: "self\\.history"
    - from: "crates/amux-daemon/src/agent/types.rs"
      to: "crates/amux-daemon/src/agent/episodic/mod.rs"
      via: "AgentConfig.episodic field of type EpisodicConfig"
      pattern: "pub episodic.*EpisodicConfig"
---

<objective>
Create the episodic memory module foundation: all data types, SQLite schema (5 tables + FTS5 index + triggers), basic CRUD operations, WORM ledger integration, privacy controls, episode link management, and wire the module into AgentEngine and AgentConfig.

Purpose: Establishes the complete data layer that Plans 02 and 03 build retrieval, counter-who, and negative knowledge logic on top of. Without this foundation, no episodic memory operations are possible.

Output: Compilable `episodic/` module with 5 source files, schema initialization integrated into HistoryStore, EpisodicStore field on AgentEngine, EpisodicConfig on AgentConfig.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-memory-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create episodic module types, schema, and HistoryStore integration</name>
  <files>
    crates/amux-daemon/src/agent/episodic/mod.rs,
    crates/amux-daemon/src/agent/episodic/schema.rs,
    crates/amux-daemon/src/agent/mod.rs,
    crates/amux-daemon/src/history.rs,
    crates/amux-daemon/src/agent/types.rs,
    crates/amux-daemon/src/agent/engine.rs,
    crates/amux-daemon/Cargo.toml
  </files>
  <read_first>
    crates/amux-daemon/src/agent/mod.rs,
    crates/amux-daemon/src/agent/engine.rs,
    crates/amux-daemon/src/agent/types.rs (lines 1100-1250 for AgentConfig, lines 2050-2100 for AgentEvent),
    crates/amux-daemon/src/agent/learning/mod.rs (reference module pattern),
    crates/amux-daemon/src/history.rs (lines 2380-2700 for init_schema pattern, lines 4730-4850 for WORM ledger),
    crates/amux-daemon/Cargo.toml,
    crates/amux-daemon/src/scrub.rs (lines 1-60 for scrub_sensitive signature)
  </read_first>
  <behavior>
    - Test 1: Episode round-trip -- create Episode struct, serialize to JSON, deserialize back, all fields match
    - Test 2: EpisodeType and EpisodeOutcome serde -- each variant serializes to snake_case string
    - Test 3: EpisodicConfig::default() returns enabled=true, episode_ttl_days=90, constraint_ttl_days=30, max_retrieval_episodes=5, max_injection_tokens=1500
    - Test 4: LinkType variants serialize correctly (retry_of, builds_on, contradicts, supersedes, caused_by)
    - Test 5: NegativeConstraint round-trip serialization
    - Test 6: CounterWhoState::default() has empty vecs and None optionals
  </behavior>
  <action>
    1. Add `rusqlite_migration = "1.3"` to `crates/amux-daemon/Cargo.toml` under `[dependencies]`.

    2. Create `crates/amux-daemon/src/agent/episodic/mod.rs` with ALL data types from the research:
       - `Episode` struct with fields: id (String), goal_run_id (Option String), thread_id (Option String), session_id (Option String), episode_type (EpisodeType), summary (String), outcome (EpisodeOutcome), root_cause (Option String), entities (Vec String), causal_chain (Vec CausalStep), solution_class (Option String), duration_ms (Option u64), tokens_used (Option u32), confidence (Option f64), created_at (u64), expires_at (Option u64)
       - `EpisodeType` enum: GoalCompletion, GoalFailure, SessionEnd, Discovery -- derive Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq with `#[serde(rename_all = "snake_case")]`
       - `EpisodeOutcome` enum: Success, Failure, Partial, Abandoned -- same derives
       - `CausalStep` struct: step (String), cause (String), effect (String) -- derive Debug, Clone, Serialize, Deserialize
       - `EpisodeLink` struct: id (String), source_episode_id (String), target_episode_id (String), link_type (LinkType), evidence (Option String), created_at (u64)
       - `LinkType` enum: RetryOf, BuildsOn, Contradicts, Supersedes, CausedBy -- same derives as EpisodeType
       - `NegativeConstraint` struct: id (String), episode_id (Option String), constraint_type (ConstraintType), subject (String), solution_class (Option String), description (String), confidence (f64), valid_until (Option u64), created_at (u64)
       - `ConstraintType` enum: RuledOut, ImpossibleCombination, KnownLimitation
       - `CounterWhoState` struct with Default derive: goal_run_id (Option String), thread_id (Option String), current_focus (Option String), recent_changes (Vec String), tried_approaches (Vec TriedApproach), correction_patterns (Vec CorrectionPattern), active_constraint_ids (Vec String), updated_at (u64)
       - `TriedApproach` struct: approach_hash (String), description (String), outcome (EpisodeOutcome), timestamp (u64)
       - `CorrectionPattern` struct: pattern (String), correction_count (u32), last_correction_at (u64)
       - `EpisodicStore` struct with Default derive: counter_who (CounterWhoState), cached_constraints (Vec NegativeConstraint), config (EpisodicConfig)
       - `EpisodicConfig` struct: enabled (bool), episode_ttl_days (u64), constraint_ttl_days (u64), max_retrieval_episodes (usize), max_injection_tokens (usize), per_session_suppression (bool) -- with Default impl (true, 90, 30, 5, 1500, false)
       - Helper method `Episode::entities_json(&self) -> String` that returns `serde_json::to_string(&self.entities).unwrap_or_default()`
       - Helper method `Episode::causal_chain_json(&self) -> String` that returns `serde_json::to_string(&self.causal_chain).unwrap_or_default()`
       - Re-export all public types and declare submodules: `pub mod schema; pub mod store; pub mod privacy; pub mod links;`
       - Add unit tests in `#[cfg(test)] mod tests` block for the behaviors listed above.

    3. Create `crates/amux-daemon/src/agent/episodic/schema.rs` with:
       - `pub const EPISODIC_SCHEMA: &str` containing the full SQL from research: CREATE TABLE IF NOT EXISTS episodes (all columns), CREATE INDEX statements (idx_episodes_goal, idx_episodes_thread, idx_episodes_type_ts, idx_episodes_outcome), CREATE VIRTUAL TABLE episodes_fts USING fts5(summary, entities, root_cause, content=episodes, content_rowid=rowid, detail=column), two FTS5 sync triggers (episodes_ai for INSERT, episodes_ad for DELETE), CREATE TABLE episode_links with indexes, CREATE TABLE negative_knowledge with indexes, CREATE TABLE counter_who_state with index.
       - `pub fn init_episodic_schema(conn: &rusqlite::Connection) -> Result<()>` that executes `EPISODIC_SCHEMA` via `conn.execute_batch()`.

    4. In `crates/amux-daemon/src/history.rs`, inside the `init_schema()` method, add a call to `super::agent::episodic::schema::init_episodic_schema(&conn)?;` AFTER the existing schema creation. Find the exact location by looking at the end of init_schema where the last `CREATE TABLE IF NOT EXISTS` block ends, and add the call there. Also add `"episodic"` to the WORM ledger kinds array (find the array of strings like `["operational", "cognitive", "contextual", "provenance"]` and append `"episodic"`).

    5. In `crates/amux-daemon/src/agent/mod.rs`, add `pub mod episodic;` in the public module declarations section (near `pub mod concierge;`, `pub mod context;`, `pub mod learning;`, etc.).

    6. In `crates/amux-daemon/src/agent/engine.rs`:
       - Add field to `AgentEngine` struct: `pub(super) episodic_store: RwLock<super::episodic::EpisodicStore>,`
       - In `new_with_storage()` constructor, add initialization: `episodic_store: RwLock::new(super::episodic::EpisodicStore::default()),` in the `Arc::new(Self { ... })` block.

    7. In `crates/amux-daemon/src/agent/types.rs`:
       - Add `EpisodicConfig` import at top: `use super::episodic::EpisodicConfig;`
       - Add field to `AgentConfig` struct: `#[serde(default)] pub episodic: EpisodicConfig,`
       - Add two new `AgentEvent` variants:
         ```
         EpisodeRecorded { episode_id: String, episode_type: String, outcome: String, summary: String },
         CounterWhoAlert { thread_id: String, pattern: String, attempt_count: u32, suggestion: String },
         ```
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p amux-daemon --lib agent::episodic -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/amux-daemon/src/agent/episodic/mod.rs` exists and contains `pub struct Episode`
    - File `crates/amux-daemon/src/agent/episodic/mod.rs` contains `pub enum EpisodeType` with variants `GoalCompletion`, `GoalFailure`, `SessionEnd`, `Discovery`
    - File `crates/amux-daemon/src/agent/episodic/mod.rs` contains `pub struct EpisodicConfig` with `pub episode_ttl_days: u64`
    - File `crates/amux-daemon/src/agent/episodic/mod.rs` contains `impl Default for EpisodicConfig` with `episode_ttl_days: 90`
    - File `crates/amux-daemon/src/agent/episodic/schema.rs` contains `CREATE TABLE IF NOT EXISTS episodes`
    - File `crates/amux-daemon/src/agent/episodic/schema.rs` contains `CREATE VIRTUAL TABLE IF NOT EXISTS episodes_fts USING fts5`
    - File `crates/amux-daemon/src/agent/episodic/schema.rs` contains `detail=column`
    - File `crates/amux-daemon/src/agent/episodic/schema.rs` contains `CREATE TABLE IF NOT EXISTS negative_knowledge`
    - File `crates/amux-daemon/src/agent/episodic/schema.rs` contains `CREATE TABLE IF NOT EXISTS counter_who_state`
    - File `crates/amux-daemon/src/agent/mod.rs` contains `pub mod episodic;`
    - File `crates/amux-daemon/src/agent/engine.rs` contains `episodic_store: RwLock<super::episodic::EpisodicStore>`
    - File `crates/amux-daemon/src/agent/types.rs` contains `pub episodic: EpisodicConfig`
    - File `crates/amux-daemon/src/agent/types.rs` contains `EpisodeRecorded`
    - File `crates/amux-daemon/Cargo.toml` contains `rusqlite_migration`
    - `cargo check -p amux-daemon` succeeds with exit code 0
  </acceptance_criteria>
  <done>All episodic data types compile, serialize/deserialize correctly in tests, schema SQL is defined, AgentEngine has episodic_store field, AgentConfig has episodic field, WORM ledger includes "episodic" kind, and `cargo check -p amux-daemon` passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement episode CRUD, WORM append, privacy scrubbing, and link management</name>
  <files>
    crates/amux-daemon/src/agent/episodic/store.rs,
    crates/amux-daemon/src/agent/episodic/privacy.rs,
    crates/amux-daemon/src/agent/episodic/links.rs
  </files>
  <read_first>
    crates/amux-daemon/src/agent/episodic/mod.rs (just created in Task 1),
    crates/amux-daemon/src/agent/episodic/schema.rs (just created in Task 1),
    crates/amux-daemon/src/agent/engine.rs (for AgentEngine struct fields, self.history pattern),
    crates/amux-daemon/src/history.rs (lines 140-200 for HistoryStore struct/conn field, lines 2700-2800 for example insert methods, lines 3188-3250 for WORM verify pattern),
    crates/amux-daemon/src/scrub.rs (for scrub_sensitive function signature),
    crates/amux-daemon/src/agent/provenance.rs (for record_provenance_event pattern -- how existing code writes to WORM)
  </read_first>
  <behavior>
    - Test 1: scrub_episode replaces API keys in summary and root_cause fields
    - Test 2: is_episode_suppressed returns true when per_session_suppression is true in config
    - Test 3: is_episode_expired returns true when expires_at < current timestamp
    - Test 4: Episode with TTL correctly computes expires_at from created_at + ttl_days * 86400 * 1000
  </behavior>
  <action>
    1. Create `crates/amux-daemon/src/agent/episodic/privacy.rs`:
       - `pub fn scrub_episode(episode: &mut Episode)` -- calls `crate::scrub::scrub_sensitive()` on `episode.summary`, `episode.root_cause` (if Some), and each entity in `episode.entities`. Modifies in place.
       - `pub fn is_episode_suppressed(config: &EpisodicConfig) -> bool` -- returns `!config.enabled || config.per_session_suppression`
       - `pub fn is_episode_expired(episode: &Episode, now_ms: u64) -> bool` -- returns true if `episode.expires_at` is Some and less than `now_ms`
       - `pub fn compute_expires_at(created_at: u64, ttl_days: u64) -> Option<u64>` -- if ttl_days == 0 return None, else return Some(created_at + ttl_days * 86400 * 1000)
       - Unit tests for all four functions.

    2. Create `crates/amux-daemon/src/agent/episodic/store.rs` with `impl AgentEngine` block:
       - `pub(crate) async fn record_episode(&self, mut episode: Episode) -> Result<()>`:
         (a) Check `is_episode_suppressed` on config from `self.config.read().await.episodic` -- if suppressed, return Ok(()) early.
         (b) Call `scrub_episode(&mut episode)` for PII redaction.
         (c) Compute `episode.expires_at` via `compute_expires_at(episode.created_at, config.episode_ttl_days)` if not already set.
         (d) Clone episode for the closure, call `self.history.conn.call(move |conn| { ... })` to INSERT into episodes table with all 15 columns using `rusqlite::params![]`.
         (e) Emit `AgentEvent::EpisodeRecorded { episode_id, episode_type, outcome, summary }` via `let _ = self.event_tx.send(...)`.
         (f) Fire WORM append: serialize episode to JSON, write to WORM ledger file at `self.data_dir.join("worm/episodic.jsonl")` following the existing WORM pattern (compute sha256 hash of previous line + current payload, append as single JSON line with seq, prev_hash, hash, timestamp, payload fields). Use `tokio::fs::OpenOptions` with append mode. If the file doesn't exist, start with seq=0 and prev_hash="0".
         (g) Return Ok(()).
       - `pub(crate) async fn get_episode(&self, episode_id: &str) -> Result<Option<Episode>>`:
         Query `SELECT * FROM episodes WHERE id = ?1`, map row to Episode struct (parse entities and causal_chain from JSON TEXT columns via `serde_json::from_str`).
       - `pub(crate) async fn list_episodes_for_goal_run(&self, goal_run_id: &str) -> Result<Vec<Episode>>`:
         Query `SELECT * FROM episodes WHERE goal_run_id = ?1 ORDER BY created_at DESC`, same row mapping.

    3. Create `crates/amux-daemon/src/agent/episodic/links.rs` with `impl AgentEngine` block:
       - `pub(crate) async fn create_episode_link(&self, link: EpisodeLink) -> Result<()>`:
         INSERT INTO episode_links with all 6 columns. Use `self.history.conn.call()`.
       - `pub(crate) async fn get_episode_links(&self, episode_id: &str) -> Result<Vec<EpisodeLink>>`:
         Query `SELECT * FROM episode_links WHERE source_episode_id = ?1 OR target_episode_id = ?1 ORDER BY created_at DESC`, map rows to EpisodeLink.
       - `pub(crate) async fn find_linked_episodes(&self, episode_id: &str, link_type: LinkType) -> Result<Vec<String>>`:
         Query both directions: `SELECT target_episode_id FROM episode_links WHERE source_episode_id = ?1 AND link_type = ?2 UNION SELECT source_episode_id FROM episode_links WHERE target_episode_id = ?1 AND link_type = ?2`.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p amux-daemon --lib agent::episodic -- --nocapture 2>&1 | tail -20 && cargo check -p amux-daemon 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/amux-daemon/src/agent/episodic/store.rs` contains `async fn record_episode`
    - File `crates/amux-daemon/src/agent/episodic/store.rs` contains `async fn get_episode`
    - File `crates/amux-daemon/src/agent/episodic/store.rs` contains `scrub_episode`
    - File `crates/amux-daemon/src/agent/episodic/store.rs` contains `is_episode_suppressed`
    - File `crates/amux-daemon/src/agent/episodic/store.rs` contains `worm` (WORM append logic)
    - File `crates/amux-daemon/src/agent/episodic/store.rs` contains `EpisodeRecorded` (event emission)
    - File `crates/amux-daemon/src/agent/episodic/privacy.rs` contains `pub fn scrub_episode`
    - File `crates/amux-daemon/src/agent/episodic/privacy.rs` contains `pub fn is_episode_suppressed`
    - File `crates/amux-daemon/src/agent/episodic/privacy.rs` contains `pub fn compute_expires_at`
    - File `crates/amux-daemon/src/agent/episodic/links.rs` contains `async fn create_episode_link`
    - File `crates/amux-daemon/src/agent/episodic/links.rs` contains `async fn get_episode_links`
    - `cargo check -p amux-daemon` succeeds with exit code 0
  </acceptance_criteria>
  <done>Episode records can be inserted via record_episode (with PII scrubbing, TTL computation, suppression check, WORM append, event emission), read back via get_episode, and linked via create_episode_link. Privacy module handles scrubbing, suppression, expiry, and TTL computation with passing unit tests.</done>
</task>

</tasks>

<verification>
1. `cargo check -p amux-daemon` compiles without errors
2. `cargo test -p amux-daemon --lib agent::episodic` -- all unit tests pass
3. `cargo test -p amux-daemon` -- all existing daemon tests still pass (no regressions)
4. Grep confirms: `episodic_store` field exists on AgentEngine, `pub episodic: EpisodicConfig` on AgentConfig, `pub mod episodic;` in agent/mod.rs, `"episodic"` in WORM ledger kinds
</verification>

<success_criteria>
- The `episodic/` module exists with 5 files (mod.rs, schema.rs, store.rs, privacy.rs, links.rs)
- All 12+ data types are defined and serializable
- SQLite schema covers all 5 tables (episodes, episodes_fts, episode_links, negative_knowledge, counter_who_state)
- Episode CRUD works through AgentEngine methods
- WORM ledger append works for episode records
- PII scrubbing applied to all episode text fields before storage
- AgentEngine and AgentConfig are wired to the episodic module
- All existing tests pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-foundation/01-01-SUMMARY.md`
</output>
