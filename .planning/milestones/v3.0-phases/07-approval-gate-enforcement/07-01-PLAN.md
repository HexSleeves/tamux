---
phase: 07-approval-gate-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amux-daemon/src/agent/dispatcher.rs
  - crates/amux-daemon/src/agent/task_scheduler.rs
  - crates/amux-daemon/src/agent/goal_planner.rs
  - crates/amux-daemon/src/agent/task_crud.rs
autonomous: true
requirements: [UNCR-08, AUTO-04]
must_haves:
  truths:
    - "Dispatcher never advances a goal run while it is AwaitingApproval."
    - "Scheduler never dispatches queued tasks whose parent goal run is AwaitingApproval."
    - "Supervised step boundaries require explicit acknowledgment before the queued child step can run."
  artifacts:
    - path: "crates/amux-daemon/src/agent/dispatcher.rs"
      provides: "Fail-closed runtime gate checks before goal-run advancement and task dispatch"
      contains: "GoalRunStatus::AwaitingApproval"
    - path: "crates/amux-daemon/src/agent/task_scheduler.rs"
      provides: "Ready-task selection that excludes tasks blocked by approval-gated parent goal runs"
      contains: "select_ready_task_indices"
    - path: "crates/amux-daemon/src/agent/goal_planner.rs"
      provides: "Supervised-mode step gate marks child step as awaiting approval before execution"
      contains: "autonomy_acknowledgment"
    - path: "crates/amux-daemon/src/agent/task_crud.rs"
      provides: "Explicit acknowledgment transition that clears supervised gate without bypassing it"
      contains: "control_goal_run"
  key_links:
    - from: "crates/amux-daemon/src/agent/dispatcher.rs"
      to: "crates/amux-daemon/src/agent/goal_planner.rs"
      via: "advance_goal_run -> enqueue_goal_run_step guard path"
      pattern: "enqueue_goal_run_step"
    - from: "crates/amux-daemon/src/agent/task_scheduler.rs"
      to: "crates/amux-daemon/src/agent/types.rs"
      via: "TaskStatus/GoalRunStatus gating in ready-task selection"
      pattern: "TaskStatus::Queued"
    - from: "crates/amux-daemon/src/agent/task_crud.rs"
      to: "crates/amux-daemon/src/agent/goal_planner.rs"
      via: "acknowledgment action unblocks supervised step and clears awaiting_approval_id"
      pattern: "awaiting_approval_id"
---

<objective>
Close the dispatcher/scheduler bypass that allowed supervised and low-confidence work to continue after a goal run entered AwaitingApproval.

Purpose: Enforce UNCR-08 and AUTO-04 at runtime state-transition boundaries, not only during planning.
Output: Unified fail-closed gating behavior across dispatcher, scheduler, goal-step enqueue, and goal control actions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-approval-gate-enforcement/07-RESEARCH.md
@crates/amux-daemon/src/agent/dispatcher.rs
@crates/amux-daemon/src/agent/task_scheduler.rs
@crates/amux-daemon/src/agent/goal_planner.rs
@crates/amux-daemon/src/agent/task_crud.rs

<interfaces>
From crates/amux-daemon/src/agent/dispatcher.rs:
```rust
pub(super) async fn dispatch_goal_runs(self: Arc<Self>);
async fn advance_goal_run(&self, goal_run_id: &str) -> Result<()>;
pub(super) async fn dispatch_ready_tasks(self: Arc<Self>) -> Result<()>;
```

From crates/amux-daemon/src/agent/task_scheduler.rs:
```rust
pub(super) fn refresh_task_queue_state(
    tasks: &mut VecDeque<AgentTask>,
    now: u64,
    sessions: &[amux_protocol::SessionInfo],
) -> Vec<AgentTask>;

pub(super) fn select_ready_task_indices(
    tasks: &VecDeque<AgentTask>,
    sessions: &[amux_protocol::SessionInfo],
) -> Vec<usize>;
```

From crates/amux-daemon/src/agent/goal_planner.rs:
```rust
pub(super) async fn enqueue_goal_run_step(&self, goal_run_id: &str) -> Result<()>;
async fn plan_confidence_gate(&self, goal_run: &GoalRun) -> PlanConfidenceAction;
```

From crates/amux-daemon/src/agent/task_crud.rs:
```rust
pub async fn control_goal_run(
    &self,
    goal_run_id: &str,
    action: &str,
    step_index: Option<usize>,
) -> bool;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed runtime gate checks to dispatcher and scheduler paths (UNCR-08)</name>
  <files>crates/amux-daemon/src/agent/dispatcher.rs, crates/amux-daemon/src/agent/task_scheduler.rs</files>
  <read_first>
    - crates/amux-daemon/src/agent/dispatcher.rs
    - crates/amux-daemon/src/agent/task_scheduler.rs
    - crates/amux-daemon/src/agent/types.rs
  </read_first>
  <behavior>
    - Test 1: dispatch_goal_runs does not select GoalRunStatus::AwaitingApproval runs for advance.
    - Test 2: dispatch_ready_tasks/select_ready_task_indices excludes queued tasks whose goal_run_id maps to AwaitingApproval.
    - Test 3: if a goal run switches to AwaitingApproval between ticks, no queued child task is moved to InProgress.
  </behavior>
  <action>
    Implement shared runtime gating at dispatch boundaries:
    1) In `dispatch_goal_runs`, update the active-run filter to exclude `GoalRunStatus::AwaitingApproval` in addition to paused/terminal states.
    2) In `advance_goal_run`, add an early fail-closed return when the loaded `goal_run.status` is `AwaitingApproval`, so direct/internal calls cannot bypass the outer filter.
    3) In `dispatch_ready_tasks`, load a goal-run snapshot and pass approval-block context into task selection (e.g., blocked goal_run_id set).
    4) Extend `select_ready_task_indices` signature to accept parent-goal gate context and skip `TaskStatus::Queued` tasks whose `task.goal_run_id` is currently awaiting approval.
    5) Update all call sites/tests for the new scheduler function signature; do not leave a default path that treats missing goal metadata as allowed for goal-linked tasks.
    6) Preserve existing lane/workspace/subagent scheduling constraints; add gate checks as an additional deny condition, not a replacement.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p tamux-daemon dispatcher:: task_scheduler:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "GoalRunStatus::AwaitingApproval" crates/amux-daemon/src/agent/dispatcher.rs` shows exclusion in active dispatch filter and early return in advance path.
    - `rg -n "select_ready_task_indices\\(" crates/amux-daemon/src/agent/task_scheduler.rs crates/amux-daemon/src/agent/dispatcher.rs` shows updated signature and caller usage with gate context.
    - At least one scheduler/dispatcher test proves a queued task tied to an AwaitingApproval goal run is not selected or dispatched.
  </acceptance_criteria>
  <done>Dispatcher and scheduler both fail closed on AwaitingApproval parent state, removing the bypass path for queued goal-step tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make supervised acknowledgment an explicit unblock transition across goal + task state (AUTO-04)</name>
  <files>crates/amux-daemon/src/agent/goal_planner.rs, crates/amux-daemon/src/agent/task_crud.rs</files>
  <read_first>
    - crates/amux-daemon/src/agent/goal_planner.rs
    - crates/amux-daemon/src/agent/task_crud.rs
    - crates/amux-daemon/src/agent/types.rs
  </read_first>
  <behavior>
    - Test 1: supervised enqueue creates/marks the current step task as AwaitingApproval before it can be dispatched.
    - Test 2: control action `resume` does not silently clear AwaitingApproval for supervised acknowledgment gates.
    - Test 3: explicit acknowledgment action transitions both goal_run and current step task from AwaitingApproval to executable states.
  </behavior>
  <action>
    Harden supervised gate semantics:
    1) In `enqueue_goal_run_step`, when `requires_acknowledgment(autonomy_level)` is true, set both goal run and current step task into approval-gated state (`GoalRunStatus::AwaitingApproval` + `TaskStatus::AwaitingApproval`) and assign/propagate a stable `awaiting_approval_id`.
    2) Ensure this happens in the same state update transaction where the task is attached to the step, so no queued window exists for scheduler pickup.
    3) In `control_goal_run`, keep `resume` scoped to paused runs only; do not allow it to clear supervised approval gates.
    4) Add explicit supervised acknowledgment action handling (e.g., `"acknowledge"` / `"ack"`) that validates current status is AwaitingApproval, clears `awaiting_approval_id`, and transitions:
       - goal run -> `Running` (or `Queued` if no steps yet)
       - current step task -> `Queued` when it was `TaskStatus::AwaitingApproval`
    5) Emit deterministic goal events for both gate-enter and gate-clear transitions so operator/audit streams reflect each significant supervised step acknowledgment.
    6) Keep existing non-supervised and non-goal tasks behavior unchanged.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo test -p tamux-daemon goal_planner:: task_crud:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "autonomy_acknowledgment|awaiting_approval_id" crates/amux-daemon/src/agent/goal_planner.rs crates/amux-daemon/src/agent/task_crud.rs` shows explicit gate enter/clear updates in both modules.
    - `rg -n "\"acknowledge\"|\"ack\"" crates/amux-daemon/src/agent/task_crud.rs` shows a dedicated acknowledgment control path.
    - Tests demonstrate that supervised step tasks are not dispatchable before acknowledgment and become dispatchable only after acknowledgment.
  </acceptance_criteria>
  <done>AUTO-04 is enforced as an explicit runtime state transition: supervised steps wait for acknowledgment and cannot be resumed implicitly.</done>
</task>

</tasks>

<verification>
- `cd /home/mkurman/gitlab/it/cmux-next && cargo check -p tamux-daemon`
- `cd /home/mkurman/gitlab/it/cmux-next && cargo test -p tamux-daemon dispatcher:: task_scheduler:: goal_planner:: task_crud:: -- --nocapture`
- `cd /home/mkurman/gitlab/it/cmux-next && rg -n "AwaitingApproval" crates/amux-daemon/src/agent/{dispatcher.rs,task_scheduler.rs,goal_planner.rs,task_crud.rs}`
</verification>

<success_criteria>
- Dispatcher and scheduler apply the same gate semantics as planning (UNCR-08) and stop advancing approval-gated work.
- Supervised runs require explicit acknowledgment before each significant step can execute (AUTO-04).
- No runnable path exists where a queued step under AwaitingApproval transitions to InProgress without acknowledgment.
</success_criteria>

<output>
After completion, create `.planning/phases/07-approval-gate-enforcement/07-01-SUMMARY.md`
</output>
